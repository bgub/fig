{
  "name": "@bgub/fig-demo-rsc",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp pack",
    "dev": "vp run --filter @bgub/fig-demo-rsc... build && sh -c 'vp pack --watch & build=$!; node --watch dist/server.js & server=$!; trap \"kill $build $server\" INT TERM EXIT; wait $server'",
    "dev:build": "vp pack --watch",
    "serve": "node dist/server.js",
    "serve:watch": "node --watch dist/server.js"
  },
  "dependencies": {
    "@bgub/fig": "workspace:*",
    "@bgub/fig-dom": "workspace:*",
    "@bgub/fig-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.0"
  }
}
